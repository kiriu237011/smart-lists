-- Флаг «разрешена ли отправка содержимого списка в AI-сервис».
--
-- DEFAULT true проставляется и существующим строкам: выключение поменяло бы
-- поведение уже созданных списков молча, а осведомлённость обеспечивается
-- интерфейсом, а не значением по умолчанию.
ALTER TABLE "List" ADD COLUMN "aiEnabled" BOOLEAN NOT NULL DEFAULT true;
