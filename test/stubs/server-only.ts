/**
 * @file server-only.ts
 * @description Заглушка пакета `server-only` для тестового окружения.
 *
 * Настоящий пакет намеренно ломает сборку, если серверный модуль попал в
 * клиентский бандл: его экспорт из `react-server`-условия бросает ошибку.
 * Vitest выполняет модули вне RSC-контекста, поэтому реальный `server-only`
 * упал бы на любом импорте `src/lib/spaces.ts` и подобных.
 *
 * Подменяется алиасом в `vitest.config.ts` и только там: на сборку приложения
 * заглушка не влияет, защита от утечки серверного кода на клиент сохраняется.
 */

export {};
